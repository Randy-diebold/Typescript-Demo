// This is a basic type definition

let greeting: string = 'Hello World';

// greeting = 2;

// we can compile this code in JavaScript using the tcs command targeting index

// you can also watch a file instead of instead of calling tsc index over and over again using the --watch flag
